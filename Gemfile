source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

ruby "2.7.4"

# Bundle edge Rails instead: gem "rails", github: "rails/rails", branch: "main"
gem "rails", "~> 7.0.4"

# Use postgresql as the database for Active Record
gem "pg", "~> 1.1"
# i thought nikifanya bundle install itaweka postgresql from the gem file
# yes itaweka but that is just a databse.. you need to have postgress installed in your machine
#lets check kama iko cause i used it last time i think i have it
# iko we just need to create a user by the name hamsa
#yah on the application ama?
# a minute
# okay
# you can test your front end now then we host

# Use the Puma web server [https://github.com/puma/puma]
gem "puma", "~> 5.0"
gem 'active_model_serializers', '~> 0.10.13'
# Use Active Model has_secure_password
gem 'bcrypt', '~> 3.1.7'

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem "tzinfo-data", platforms: %i[ mingw mswin x64_mingw jruby ]

# Use Rack CORS for handling Cross-Origin Resource Sharing (CORS), making cross-origin AJAX possible
gem "rack-cors"

group :development, :test do
  # See https://guides.rubyonrails.org/debugging_rails_applications.html#debugging-with-the-debug-gem
  gem "debug", platforms: %i[ mri mingw x64_mingw ]
  gem 'byebug', '~> 11.1', '>= 11.1.3'
end

group :development do
  # Speed up commands on slow machines / big apps [https://github.com/rails/spring]
  # gem "spring"
end


gem "ruby-lsp", "~> 0.3.6", :group => :development


# install postgress in your machine

# alternatively is you not insecure you can share the heroku logins i try

#okay lemme install postgress
# great
#wacha niangalie logins then i send

# you were typing something?
